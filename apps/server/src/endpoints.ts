export enum ENDPOINTS {
  ME_FOLLOWING = '/me/following',
  ARTISTS = '/artists',
  ALBUMS = '/albums',
  PLAYLISTS = '/playlists',
  USERS = '/users',
}

export enum FETCH_TYPES {
  ARTISTS = 'artist',
  ALBUMS = 'albums',
  SINGLE = 'single',
  TRACKS = 'tracks',
  PLAYLISTS = 'playlists',
  GROUPS = 'include_groups',
}
